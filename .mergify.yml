queue_rules:
  - name: default
    merge_conditions:
      - base=main
    commit_message_format:
      title: pr-title
      body: pr-body

pull_request_rules:
  - name: qa-engine-alive
    conditions:
      - base=main
    actions:
      comment:
        message: "ENGINE-ALIVE {{ number }}"

queue_rules:
  - name: default
    merge_conditions:
      - base=main
    commit_message_format:
      title: pr-title
      body: pr-body

commands_restrictions:
  queue:
    conditions:
      - sender = asilboyka170-crypto

pull_request_rules:
  - name: never
    conditions:
      - base = zzz-no-such-branch
    actions:
      comment:
        message: never
